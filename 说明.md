# E:\dshwork 目录说明

整理于 2026-09-15。

**只归拢了 WinQuad 相关的东西**，别的项目全部留在顶层原位没动。

```
E:\dshwork\
  winquad\        ← 源码仓库（Git，唯一需要动的地方）
  winquad存档\     ← WinQuad 的发布包、开发归档、分析图
  说明.md

  ── 以下是别的项目，保持原样 ──
  cleanup-audit\  dsh-restart\  shots\  sumdongfeng\
  sw-diag\  sw-diff\  sw-fix\  报告草稿\
  _bakdump.py  _bakdump.txt  _fontonly.py  _verify.py
  dsh-routed-subagent-0.3.*.tgz
  ps14_1.png  ps14_2.png  pssp26.png
  SolidWorks2021-诊断与修复报告.md
```

---

## winquad\

源码仓库，`origin` 指向 <https://github.com/Frozen1096/WinQuad>。

发布流程（v0.2 走的就是这套）：

1. 改 `overlay/WinQuad.csproj` 和 `manager/WinQuad.Manager.csproj` 的版本号
2. `dotnet build` 两个项目
3. 拷进本存档的 `packages\WinQuad-分享版\`，`dotnet publish` 出便携版
4. 同步 README / LICENSE / 使用说明 / docs 到两个包
5. 压成 zip 放到 `packages\release-vX.Y\`
6. `git push` + `git tag` + `gh release create`

`bin/` 和 `obj/` 占了大部分体积，删掉随时能重建。

---

## winquad存档\

### packages\ —— 分发包

| 目录 | 是什么 |
|---|---|
| `WinQuad-分享版\` | framework-dependent，约 840 KB，需要 .NET 10 桌面运行时 |
| `WinQuad-便携版\` | self-contained 单文件，约 93 MB，解压即用 |
| `release-v0.1\` | v0.1 的两个 zip + release-notes.md |
| `release-v0.2\` | v0.2 的两个 zip + release-notes.md，已发到 GitHub |

发布用的 zip 就是从这里压出来的，改完包记得重新 `Compress-Archive`。

### dev\ —— 开发过程产物

| 目录 / 文件 | 是什么 |
|---|---|
| `dev-archive\` | 早期探索：旧 dist、第三方参考仓库、19 张调试截图、探索脚本、mockup |
| `dev-archive\*.ps1` | 一轮轮测试脚本（字体、外观、投影、图标、渲染方式……），每个都写了在测什么 |
| `dev-archive\font-*.ps1` / `shadow-*.ps1` / `*-test.ps1` | 见文件名 |
| `memtest\` | .NET 内存基线：一个什么都不做的 WinForms 空窗口，用来算「程序自己占多少内存」 |
| `icon-实验-*.png` | microwin.exe 图标的诊断过程（为什么宫格里只剩一片白） |
| `V4.0 STEP 7 MicroWIN SP9*.ico/png` | 手绘的 24×24 简化图标（程序缩到 24px 后认不出来时的备选） |
| `extract_icon.py` / `make_24x24_icon.ps1` | 上面那个图标的提取和生成脚本 |

### images\ —— 分析对比图

设计决策都是靠这些图定下来的，翻旧账时有用。

| 图 | 记录了什么 |
|---|---|
| `字体对照.png` | 8 种字体在 7pt / 37px 格子里的真实渲染（放大 8 倍看像素） |
| `字体方案对比.png` | F0–F5 六种文字配色方案 |
| `投影对照.png` | 描边 vs 投影，浅底深底各一组 |
| `投影加描边.png` | 投影和描边叠加的四种组合 |
| `花边修复.png` | 白字笔画末端 ClearType 彩边修复前后 |
| `文字彩边检查.png` / `真实文字放大.png` | 真实桌面上的文字放大 10–12 倍，看彩边 |
| `渲染参数对照.png` | 清屏色、位图格式、TextRenderingHint 对彩边的影响 |
| `抗锯齿对比.png` / `字渲染方式.png` | 灰度抗锯齿 vs 无抗锯齿 |
| `图标对比.png` | 桌面原图标 vs 宫格内图标（发现颜色失真那次） |
| `图标修复后.png` | 上面那个修好之后 |
| `宫格外观对比.png` / `宫格外观对比2.png` | 底板/边框的几种外观方案 |
| `图标配色对比.png` ×3 | 应用图标本身的配色迭代 |
| `当前效果.png` / `深色文字效果.png` | 自用配置的实际观感 |
| `右键菜单.png` | 右键菜单条目 |
| `_footprint.png` | 「占 2×2 图标格 + 里面 3×3」的预览 |
| `_scale.png` | 自动等比生效后的同一个宫格 |
| `_slicer.png` / `_fit.png` | 1×2 形状自适应前后 |
| `_settings*.png` / `_main*.png` | 管理器界面的离屏渲染（`--shot` 出的） |

---

## 别的项目（留在顶层，没动过）

跟 WinQuad 无关，归拢时**没有移动它们**，保持原位：

| 目录 / 文件 | 是什么 |
|---|---|
| `sw-diag\` `sw-diff\` `sw-fix\` `shots\` | SolidWorks 2021/2024 的诊断、配置对比、修复脚本和截图 |
| `SolidWorks2021-诊断与修复报告.md` | 上面那件事的结论 |
| `sumdongfeng\` | 夏季学期生产实习报告及素材 |
| `报告草稿\` | 报告草稿与插图 |
| `cleanup-audit\` | 一次磁盘清理的审计脚本和报告 |
| `dsh-restart\` | DSH 重启相关脚本 |
| `dsh-routed-subagent-0.3.*.tgz` | DSH 的 npm 包 |
| `_bakdump.py` / `_bakdump.txt` / `_verify.py` / `_fontonly.py` | 早期的一次性脚本，用途已不明 |
| `ps14_*.png` / `pssp26.png` | 来源不明的截图 |

---

## 这次整理删掉了什么

都是能随时重建或明显是中间产物的：

| 删掉的 | 大小 | 为什么 |
|---|---|---|
| `_gtest\` `_shot\` | 各约 123 MB | 测试用的安装目录副本，从仓库重新部署即可 |
| `_pub\` | 约 103 MB | `dotnet publish` 的中间输出 |
| `_desk/_desk2/_now/_now2/_opaque/_after/_prog/_prog-crop.png` | 约 19 MB | 原始桌面截图；有用的都另存成有名字的对比图了 |
| 根目录的 `WinQuad.Manager.exe` 等 4 个文件 | 约 357 KB | 误放的二进制副本，真身在 `winquad\manager\bin` 和 `D:\WinQuad` |
| `config.json` | 0 字节 | 空文件 |
| `_settings2.png` | 57 KB | 和 `_settings.png` 逐字节相同 |

共释放约 **370 MB**，dshwork 总占用从 0.95 GB 降到 0.60 GB。

---

## 没有动的东西

- `winquad\` 仓库一个字节没改（`git status` 干净）
- `D:\WinQuad\` 那份正在运行的安装目录完全没碰
- 没有任何文件被移动出 `E:\dshwork`
- **非 WinQuad 的项目全部留在顶层原位**，一个都没挪（一度归到 `其他项目\`，
  随后按要求原样放回了）
