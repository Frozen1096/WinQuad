# 更新日志

本文件记录每一次改动。**已推送**和**未推送**分开写，
「未发布」一节里的东西还在本地改着，等体验确认后再集中推上去。

---

## 未发布

以下改动已完成、已在本机生效，但**尚未推送到 GitHub**。

### 新增：文字投影（`shadowOffset` / `shadowAlpha` / `shadowColor`）

**起因**：用户观察到 Windows 自己的桌面图标标签用的是**投影**，不是描边。

这个观察是对的，而且差别很大：

- **描边**：四/八个方向各画一遍暗色，字的四周全被包住 → 笔画被"撑胖"，字腔（笔画间的空隙）被挤死
- **投影**：只在**右下**垫一份暗色副本 → 字的形状完整保留，只在一侧补对比度

放大 8 倍对比（对照图 `E:\dshwork\投影对照.png`）看得最清楚：同样一句「搜索」，
描边那版两个字的内部结构全糊了，投影那版扌和叟清清楚楚。

**实现**：`overlay/Program.cs` 与 `manager/PreviewPainter.cs` 的 `DrawOutlinedText`。
顺序固定为「投影 → 描边 → 正文」，投影在最底层。两个**可以同时开**，也可以各自单独用。

新增三个配置项，老配置没有这三个字段时走默认值（`shadowOffset: 0`，即关闭），向后兼容。

**顺带发现**：把文字衬托完全关掉时（`投影对照.png` 的 S5 那行），屏幕上出现彩色噪点
—— 那是 ClearType 的次像素着色。白字压在浅色底板上时对比度趋近于零，
剩下的只有亚像素彩噪。**这从渲染层面证实了「白字 + 浅底」是死路**。

### 自用配置：文字改成「投影 2px + 描边 1px」

`D:\WinQuad\config.json` 的 `style` 段（在 F3 基础上继续改）：

| 项 | F3 | 现在 | 为什么 |
|---|---|---|---|
| `shadowOffset` | （无此字段） | **2** | 新增。右下 2px 投影，给字垫出对比度 |
| `shadowAlpha` | （无此字段） | **200** | 投影透明度 |
| `outlineWidth` | 1 | 1 | 不变，保留一圈边缘定义 |
| `outlineAlpha` | 190 | **255** | 描边的活是定边界，给满；对照图里 255 明显比 190 有定义感 |

**还没解决的问题**：底板仍是浅色（白 `alpha 80`）、文字仍是白色。
投影和描边都只是在"补"，没有改变"白压白"这个基本面。
真正的解法是二选一——**深色底板配白字**，或者**浅色底板配深字**。
对照图见 `E:\dshwork\宫格外观对比2.png`。

**另一个待定项**：`fontBold` 目前仍是 `true`。8 种字体的对照渲染
（`E:\dshwork\字体对照.png`）显示，**微软雅黑 7pt 加粗是全场最差的一档**，
笔画粘连严重；取消加粗立刻好转。这项还没动，等外观整体定了一起改。

**字体可选方案**（本机装了的）：
- `MiSans` / `Noto Sans SC` —— 字形最好，但**不是 Windows 自带**，分享出去对方大概率没有
  （`new Font()` 遇到缺失字体不会报错，会静默回退成通用字体，很难排查）
- `DengXian` 等线 —— Windows 10+ 自带，比雅黑细、小字号下干净，**推荐做分享版默认值**

### 自用配置：字体描边调整（F3 方案）

`D:\WinQuad\config.json` 的 `style` 段：

| 项 | 改前 | 改后 | 为什么 |
|---|---|---|---|
| `outlineWidth` | 2 | **1** | 2px 纯黑描边把笔画缝隙糊住了，放大看「小飞机」是一团黑底白线 |
| `outlineAlpha` | 255 | **190** | 描边全不透明太重，和浅色底板（`plateAlpha 80`）打架 |
| 其余 | — | 不变 | `textColor` 保持白色、`fontBold` 保持加粗、字号仍 7pt |

**背景**：白字压在浅色底板上，本来就得靠描边才读得出来。
真正干净的解法是换成深色文字（浅底配深字是天然对比，不需要描边），
实测的 F1/F4 两个方案都比现在清爽。但这次先按 F3 走，
保留白色文字的风格，只把描边收细。

**备选方案**（对照图存在 `E:\dshwork\字体方案对比.png`，随时可以换）：
- F1 深字 `#1A1A1A` + 无描边 —— 最干净
- F4 深字 + 底板提到 110 —— 对比最强
- F5 深字 + 关掉加粗 —— 笔画偏细，不建议

### 自用安装的文档补齐

`D:\WinQuad` 之前只有程序，文档没跟上：

- 补 `README.md`（8,155 字节）
- 补 `LICENSE`（35,803 字节）
- 补 `docs\` 三张界面截图
- `使用说明.txt` 从 09:25 旧版换成最新（8,845 字节）

三份文档与 `WinQuad-分享版` 里的逐字节一致（MD5 校验通过）。

---

## v0.1 — 2026-09-13

首个公开测试版。定位是「给朋友试用的测试初版」。
版本号始终是 `0.1.0.0`，中途补进去的改动没有单独发版。

### 核心功能

- **桌面四宫格**：用 `SetParent` 把自绘窗口嵌进 Windows 桌面图标层
  （`SHELLDLL_DefView`），所以它和真图标同层 —— 被所有窗口盖住、
  按 `Win+D` 时还在、不占任务栏、不进 `Alt+Tab`。
- **图标网格实测**：跨进程读 `SysListView32` 里每个图标的真实坐标，
  算出格距（本机 83 × 114）。不读注册表 —— 实测注册表纵向值给 90，和真实值差很多。
- **位置按格位存**：`col` / `row` 而不是像素坐标，换分辨率不会跑偏。
- **拖动吸附 + 自动避让**：松手对齐图标网格；目标格位被占就挪到最近空位。
- **管理器**：三栏图形界面，支持从资源管理器拖放添加程序，
  `.lnk` 自动追溯成真正的 `.exe`（因此图标没有快捷方式小箭头）。
- **参数设置**：一屏可滚动，顶部实时预览，参数超限实时报红。
- **配置带注释**：`config.json` 与 `group-*.json` 里以 `_` 开头的字段就是注释，
  程序忽略。管理器保存时**就地替换数值**，注释和排版原样保留（有回归测试守着）。

### 中途补进 v0.1 的改动

按时间顺序：

1. **改名 DeskGrid → WinQuad**，程序名、exe 名、窗口标题、日志名、注册表自启项名全部跟着改；
   新增旧自启项清理逻辑（改名前设过自启的机器上会残留一个指向老路径的死项）。
2. **README 重写**：去掉 AI 腔（排比句、emoji 小标题、过量加粗与破折号）、
   去掉界面文字描述（只留截图）、许可章节收缩成一行。
3. **应用图标**：白底圆角方块 + 四宫格，左下格是浅蓝底 + 蓝箭头的「快捷方式」格。
   四色沿用红绿黄蓝的骨架但取值重新配过，没有照抄任何厂商的官方品牌色。
   源文件 `assets/icon.svg`，配 `render-icon.ps1` + `make-ico.ps1` 两个可重跑的脚本。
4. **提权加固**：给覆盖层补 `app.manifest` 显式声明 `asInvoker`。
   之前是靠 SDK 默认值（隐式保证）。覆盖层要 `SetParent` 挂到 explorer 的桌面窗口上，
   被提权会被 UIPI 拦掉；管理器提权后则收不到拖放。
5. **许可**：CC BY-NC-SA 4.0 → PolyForm Noncommercial 1.0.0 → MIT → **GPL-3.0**。
   前三者都不合适：CC 不是为软件写的、PolyForm 和 CC 都禁止商用（也就都不是开源协议）、
   MIT 允许别人做成闭源商业产品。GPL-3.0 是「允许商用 + 强制开源」，正好。

### 已知限制

- 只在作者一台机器上验证过（Windows 11 / 1920×1080 / 100% 缩放）。
  换分辨率、换缩放比例、多显示器、其他 Windows 版本都可能出问题。
- 四宫格会把图标缩到 24×24，细节多的图标缩小后可能认不出来，
  建议自行重绘一版小尺寸专用的简化图标。
- 配置格式后续可能还会改，升级时旧配置不保证兼容。
