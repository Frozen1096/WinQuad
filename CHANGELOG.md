# 更新日志

本文件记录每一次改动。**已推送**和**未推送**分开写，
「未发布」一节里的东西还在本地改着，等体验确认后再集中推上去。

---

## 未发布

以下改动已完成、已在本机生效，但**尚未推送到 GitHub**。

### 宫格的「占地」与「形状」拆成两层（每宫格单独设置）

用户要的是「不局限于一个图标的宽度，尝试在多个图标范围内（1×2、2×1、2×2 等），
放入更多的图标（比如在 2×2 里放入 3×3）」，
并且明确要求**放进每个宫格自己的设置里**（第三列下方）。

**核心是把原来混在一起的两件事拆开**：

| 概念 | 字段 | 含义 |
|---|---|---|
| **占地** | `layout.footprintCols` / `footprintRows` | 宫格**占几个桌面图标格**（每格 = 图标格距 83×114）|
| **形状** | `layout.cols` / `layout.rows` | 宫格**里面装几列几行格子** |

两者独立，所以可以**占 2×2 个图标格、里面放 3×3 共 9 个格子**。

**尺寸怎么算**：
- 指定了占地 → 外框 = 占用格数 × 图标格距，再让出 `bleed`，
  使「外框 + bleed」正好是整数个图标格，和桌面图标格对齐；格子再把外框等分。
- 没指定（null）→ 老行为：外框按总配置那套行列数算出的标准格位，格子数少了就摊开。

**实测**（离屏渲染，占 2×2 + 里面 3×3）：

```
外框 162 × 222　单格 52×72 × 9 格
```

**管理器**：「位置」组里现在是两行 ——

```
占用    [2] 格宽 × [2] 格高    [自动]
形状    [3] 列 × [3] 行        [跟随总配置]
占用：2 × 2 个图标格　形状：本宫格单独设置（3 列 × 3 行）
外框 162 × 222　单格 52×72 × 9 格
```

「占用」的「自动」按钮把 footprint 清空回自动；「形状」的按钮把 cols/rows 清空回跟随总配置。

**顺带**：跨格占用现在是**故意**的，所以覆盖层那句「判定框大于图标格距」的警告
只在**没显式指定占地**时才打。同时日志里多打一行实际占用几个图标格。

8 个 `group-*.json` 都补上了 `footprintCols` / `footprintRows`（值都是 `null`，行为不变），
备份在 `*.bak-fp-*`。

**还没做的一件事**：格子变大之后（52×72），里面的图标还是 24px、文字还是 16px 高，
留白偏多。要不要让图标和文字跟着格子一起放大，等用户看过效果再定。

### 格子数变少时，格子摊开占满外框（而不是宫格缩小）

用户把 SLICER 改成 1 列 × 2 行之后反馈「希望它能自适应占满整个格子，对长名字会友好些」。

**上一版的行为是错的**：格子尺寸固定 37×48，宫格外框跟着缩 ——
2×2 改成 1×2 之后宫格从 **79 宽缩到 41 宽**，变成一条窄缝。
而用户改形状的**目的恰恰是想给长名字留位置**，结果适得其反。

**现在**：宫格外框按**总配置那套行列数**算出来的「标准格位」保持不变，
格子数少了就每格摊大去填满：

| 形状 | 宫格 | 单格 |
|---|---|---|
| 2×2 | 79 × 101 | 37 × 48（和以前一样）|
| **1×2** | **79 × 101** | **75 × 48**（宽格子，`orca-slicer` 完整显示）|
| 2×1 | 79 × 101 | 37 × 97（高格子）|

实测（重启后的日志）：SLICER 从 `41×101` 变成 `79×101`，8 个宫格全部 79×101，碰撞检查全过。

实现还是在 `LayoutMetrics.From()` 和 `SizeSection.WithLayout()` 里那两处 ——
外框尺寸由总配置算，格子尺寸再把外框按自己的行列数等分。

### 每个宫格可以有自己的形状（行列数）

用户要的是「每个宫格可以单独调整，参数框放在第三列下那一小块地方」。

**之前**：行列数只有一份，在 `config.json` 的 `size.cols` / `size.rows`，**所有宫格共用**。
设成 1×2 就是所有宫格都变竖条。

**现在**：`group-*.json` 里多了一段 `layout`：

```json
"layout" {
  "cols": null,
  "rows": null
}
```

两个都填 `null` 就跟随总配置，行为和以前完全一样（向后兼容，老文件不用改）。
填了数字就只影响这一个宫格 —— 可以一个 2×2、一个 1×2、一个 2×1 混着放。

**管理器**：「位置」组里加了一行「形状」：`[1] 列 × [2] 行  [改回跟随总配置]`，
下面一行彩色提示说明现在是自己定还是跟随总配置，并显示**实际宽高和判定框尺寸**。
按钮在「改为单独设置 / 改回跟随总配置」之间切换。

**一处关键实现选择**：没有在每处单独判断行列数，而是加了
`SizeSection.WithLayout(LayoutSection)` —— 把总配置的尺寸和宫格自己的行列数
**合成一份「实际生效」的 SizeSection**。宫格宽高、格子矩形、判定框、预览尺寸
全是从 SizeSection 算出来的，合成之后所有下游代码一行都不用改，
也就不会出现「某处忘了跟随宫格行列数」这种漏网。

### 空置的格子彻底透明

原来所有格子都画底板，空格子只是"不画图标文字"：

```csharp
// 旧代码
if (string.IsNullOrWhiteSpace(item.Path)) continue;   // 底板在上面已经画完了
```

于是 2×2 里空的那两格仍然顶着两块半透明底，看着像四个格子却只有两个能用。
现在空置的格子**底色、边框、图标、文字全都不画**，整块透出桌面。

窗口区域（`SetWindowRgn`）仍然覆盖整块，所以最外圈的拖动环在哪都能拖。

### 管理器第三列的打磨

1. **格子列表按行列数裁剪** —— 设成 1×2 就只列两行，不再固定四行让后两行点了没反应
2. **「＋ 添加程序」和拖放的容量**改成看这个宫格自己的行列数，且不再往容量外追加
3. **「移除」改成清空该格**，不再把行删掉（删行会让列表短于宫格形状）
4. **「上移/下移」的上界**用当前宫格的格子数，不会换到被行列数藏起来的槽位

### 修复：`LayoutPreview` 的宽度判重会吞掉形状变化

`LayoutPreview(hostW)` 靠 `if (hostW == _lastLayoutW) return;` 防抖
（早期版本会自己触发自己，日志被刷上百次）。但改行列数时**宽度没变**，
不强制就会被直接跳过，预览框高度不跟着更新。加了 `force` 参数和 `RelayoutPreview()`。

### 顺带修：`SetValue` 现在能补出不存在的整段

之前只会补"段内缺失的字段"，段本身不存在就直接返回。`group-*.json` 原本没有
`layout` 段，不补的话每宫格行列数永远存不进去。现在会插一个段出来，
位置在根对象最后一个属性之后，逗号自动处理。

所有 8 个 `group-*.json` 已经补上了带 `_说明` 注释的 `layout` 段（`cols`/`rows` 都是 `null`，
行为不变）。回归测试也扩了：

```
载入时 layout = null x null
保存后文件里有 layout 段: True
回读 layout.cols = 3  (期望 3)
回读 layout.rows = 1  (期望 1)
```

### 设置界面：透明度改滑条，新增投影选项

**透明度全部改成滑条**（`SettingsForm.AlphaSlider`）。原来都是数字框，要精确敲；
而透明度是"拖到看着顺眼"的参数，滑条能边拖边看上面的实时预览，右边跟一个数字方便复现。
涉及的项：底板透明度、悬停时底板透明度、边框透明度、描边透明度、投影透明度。

**新增投影三项**：投影偏移（0~6）、投影透明度、投影颜色。
和原有的描边三项放在一起，「外观」组末尾加了两行说明，讲清两者可以叠加、顺序是「投影 → 描边 → 正文」。
滑条上挂了 ToolTip，鼠标停上去能看到这项具体管什么。

### 管理器：总配置的默认底色/透明度可以直接改

用户的原话：「每一格的透明度和颜色应该有一个默认值（总配置中的值），
现在只能同步它（跟随总配置按钮），不能快速修改这个默认值」。

**改法**：第三列的「底色」和「透明度」两个控件，**改哪个取决于当前格处于什么模式**——

- 这一格**跟随总配置** → 改的是 `config.json` 里的 `style.plateColor` / `style.plateAlpha`，
  也就是所有格子的默认值。不用开参数设置对话框。
- 这一格**单独设置** → 改的才是这一格自己的覆盖值。

按钮文字随模式变（「改为本格单独设置」/「改回跟随总配置」），
下面还有一行彩色提示说明现在改的范围，避免误改到全局。

**顺带修了一个隐藏 bug**：`ApplyFieldsToCurrentItem` 里原来是
`it.BgAlpha = _bgAlpha.Value;` —— **无条件写**。也就是说只要保存过一次，
「跟随总配置」就被固化成当前值的覆盖值，之后再改总配置这一格也不会跟着变了。
现在跟随状态下写回 `null`。

### 修复：总配置补字段时静默失败

`PatchConfig` 的 `SetValue` 在**键不存在时直接原样返回**。后果很隐蔽：
程序新加的配置项（比如这次的投影三项），在旧 `config.json` 上怎么改都存不进去，
**而且完全没有报错**。

现在键不存在会**插进去**：插在段落开头，缩进照抄段内第一行，空段落 `{}` 单独处理。
回归测试也扩了 —— 现在会拿一份**故意删掉投影字段**的配置跑一遍，
验证三个字段都能补进去且值正确回读。

```
保存前注释字段数 = 48      保存后注释字段数 = 48      注释还在: True
回读 shadowOffset  = 5    (期望 5)
回读 shadowAlpha   = 177  (期望 177)
回读 shadowColor   = 1,2,3 (期望 1,2,3)
★ 通过
```

### 新增：`--shot` 离屏渲染界面

改界面时想确认排版，又不想弹窗打断正在用电脑的人，所以加了：

```
WinQuad.Manager.exe --shot main|settings 输出.png [高度]
```

用 `CreateControl` 递归建句柄、反射调用各自的排布方法（设置界面是 `LayoutAll()`、
主界面是 `LoadAll()`），再 `DrawToBitmap`。窗口全程不显示。

**踩到的三个坑**（都写在代码注释里了）：
1. `LoadAll` / `LayoutAll` 不是 WinForms 自动排的，是各自的私有方法按窗口宽度算的，
   只在 Load/Resize 里调用 —— 窗口不显示就永远不触发。必须反射直接叫。
2. 不建句柄的话 `DrawToBitmap` 画出来是一片空白。
3. 窗口高度被 `MaxWindowTrackSize`（屏幕高 + 边框 = 1100）卡住，`AutoScrollPosition`
   又需要窗口可见才生效 —— 所以传高度时干脆改渲染滚动区里的内容面板，它的尺寸就是完整内容高度。

### 修复：图标颜色失真（只剩一片白）

用户反馈宫格里的图标颜色不对。实测 `program` 宫格里 `microwin.exe` 的图标：

| | 平均亮度 | 最暗 | 最亮 |
|---|---|---|---|
| 桌面原图标 | — | 0（纯黑） | 255 |
| 宫格内（修复前） | 236 | **143** | 255 |
| 宫格内（修复后） | **134** | **0** | 255 |

修复前最暗只有 143 —— 深色部分整个丢了，只剩一块白色窗口，机身完全不见。

**根因**：`overlay/Program.cs` 的 `DrawIconTight` 用 `g.DrawIcon(icon, rect)` 绘制。

1. **`DrawIcon` 会把整个图标缩放到目标矩形**，传进去的源范围没人理 ——
   所以 `TightContent` 辛苦算出来的紧边界一直是白算的，
   图标四周自带的透明边距照旧占着地方。
2. `DrawIcon` 对带 Alpha 的图标处理不可靠，颜色会失真。

**修复**：改成 `icon.ToBitmap()` + `g.DrawImage(bmp, destRect, content, Pixel)`，
源矩形显式传 `content`。两个问题一起解决，顺带让紧边界裁剪真正生效。
管理器那边的 `PreviewPainter.TightDrawIcon` 本来就是这么写的，所以只有覆盖层有这个毛病。

**一个意料之外的观察**：这个问题在**独立测试里复现不出来** ——
单独调 `ExtractIconEx` + `DrawIcon(24×24)` 得到的最暗值是 0，一切正常。
只有放进覆盖层真实的绘制流程才失真。所以「隔离测试通过」不等于「线上没问题」。
排查过程中还顺带确认了几件不是原因的事：图标索引没取错（`microwin.exe` 里 171 个图标，
索引 0 正确）、`TightContent` 算出的边界是完整的 32×32、
图标本身 Alpha 是干净的 128 个 0 + 896 个 255（1 位掩码图标）。

### 修复：白字笔画末端的彩色花边

用户反馈「这版除了字体末端有花边，观感已经很好了」。花边就是 ClearType 次像素渲染
在笔画边缘留下的黄/青/品红彩点。

**根因**：`TextRenderer.DrawText` 画到带 Alpha 的离屏位图上时，若不给
`Graphics.TextRenderingHint`，它会按系统设置走 ClearType。
（系统确实开着：`FontSmoothingType = 2`。）

**修复**：在离屏 `Graphics` 上显式指定灰度抗锯齿，覆盖层和 `PreviewPainter` 各一行：

```csharp
bg.TextRenderingHint = System.Drawing.Text.TextRenderingHint.AntiAliasGridFit;
```

**实测**（白字 + 投影 2px + 描边 1px，文字带内 RGB 极差 > 20 的像素数）：

| | 修复前 | 修复后 |
|---|---|---|
| 离屏实验（592 像素） | 148 个，极差 187 | **0 个，极差 4** |
| 真机桌面（1078 像素） | 242 个，极差 187 | **0 个，极差 8** |

**试过但没用**的几条路，一并记下来免得以后重走：

- 换离屏位图的**清屏色**（`Transparent` / 透明白 / 透明灰）—— 三者结果完全一样，都是 148
- `SingleBitPerPixelGridFit` —— 仍然 148
- 离屏位图换成**不透明的 `Format32bppRgb`** —— 仍然 148
- 干脆不用离屏位图、直接画到窗口 DC —— 同样有彩边

**顺带搞清的一件事**：黑字在任何组合下都是 0 彩噪（四种配置各测过），
白字在任何组合下都是 148/242。所以这个花边**只跟文字颜色有关**，与描边、底板透明度都无关。

### 自用配置：投影 + 描边（当前状态）

用户要的是「S4 加 1px 描边」，即投影 2px/200 配 1px 描边：

| 项 | 值 |
|---|---|
| `textColor` | `[255,255,255]` 白字 |
| `outlineColor` / `outlineAlpha` / `outlineWidth` | `[0,0,0]` / 190 / **1** |
| `shadowOffset` / `shadowAlpha` | **2** / 200 |
| `fontBold` | `false` |

> **过程更正**：我一度把文字改成深色并准备保留，用户指出理解错了，
> 要求回滚。回滚时发现配置在之前几轮的测试脚本里被还原成了更早的值
> （`outlineWidth` 是 2 而不是我汇报的 1），已按用户原话改回 1。
> 配置备份都留在 `D:\WinQuad\config.json.bak-*`，可随时切换。

### 重要发现：文字彩噪来自「白色文字」，不是 ClearType

（这一段是上面那次修复之前的排查过程，结论已被上一条取代，但实验数据值得留着。）

| 配置 | 彩色像素（文字带内）|
|---|---|
| 半透明底 + **黑字** + 无描边无投影 | **0 / 1078** |
| 不透明底 + **黑字** + 无描边无投影 | **0 / 1264** |
| 半透明底 + **黑字** + 有描边 | **0 / 1078** |
| 半透明底 + **白字** + 无描边无投影 | 242 / 1078 |
| 不透明底 + **白字** + 无描边无投影 | 242 / 1078 |
| 半透明底 + **白字** + 只开描边 | 242 / 1078 |
| 不透明底 + **白字** + 描边 + 投影 | 242 / 1078 |

**黑白字各测了四种组合，黑字全是 0，白字全是 242。**

### 右键菜单：补齐「像正常快捷方式一样」的操作

`overlay/Program.cs` 的 `BuildContextMenu` 改成每次弹出前重建，按**右键点在哪一格**给条目：

- **打开 <名字>**（加粗，排第一）
- **打开文件位置** —— `explorer.exe /select,"路径"`，定位并选中
- **属性** —— `ShellExecuteEx` + `SEE_MASK_INVOKEIDLIST` + `lpVerb="properties"`，
  和资源管理器右键「属性」弹的是同一个原生多标签页
- **用 WinQuad 管理器打开配置** —— 启动同目录的 `WinQuad.Manager.exe`
- 后面接原有的位置/配置/重新载入/日志/退出

右键落在空白处（没命中格子）时，前三项不出现。
格子里的路径不存在（程序被卸载或挪走）时也不出现 —— 不会给出点了没反应的菜单项。

### 待办：设置界面里的投影/描边选项

用户要求：
1. 把投影和描边的选项加进「参数设置」
2. 各种透明度改成**滑条**（现在 `SettingsForm` 里还是数字框）
3. **每一格的透明度和颜色要能在总配置里快速改默认值** ——
   现在只能靠第三列的「跟随总配置」按钮往总配置同步，
   改默认值本身得开参数设置对话框，不够快

这三项**还没做**。

### 新增：文字投影（`shadowOffset` / `shadowAlpha` / `shadowColor`）

**起因**：用户观察到 Windows 自己的桌面图标标签用的是**投影**，不是描边。

这个观察是对的，而且差别很大：

- **描边**：四/八个方向各画一遍暗色，字的四周全被包住 → 笔画被"撑胖"，字腔（笔画间的空隙）被挤死
- **投影**：只在**右下**垫一份暗色副本 → 字的形状完整保留，只在一侧补对比度

放大 8 倍对比（对照图 `E:\dshwork\投影对照.png`）看得最清楚：同样一句「搜索」，
描边那版两个字的内部结构全糊了，投影那版扌和叟清清楚楚。

**实现**：`overlay/Program.cs` 与 `manager/PreviewPainter.cs` 的 `DrawOutlinedText`。
顺序固定为「投影 → 描边 → 正文」，投影在最底层。两个**可以同时开**，也可以各自单独用。

新增三个配置项，老配置没有这三个字段时走默认值（`shadowOffset: 0`，即关闭），向后兼容。

**顺带发现**：把文字衬托完全关掉时（`投影对照.png` 的 S5 那行），屏幕上出现彩色噪点
—— 那是 ClearType 的次像素着色。白字压在浅色底板上时对比度趋近于零，
剩下的只有亚像素彩噪。**这从渲染层面证实了「白字 + 浅底」是死路**。

### 自用配置：文字改成「投影 2px + 描边 1px」

`D:\WinQuad\config.json` 的 `style` 段（在 F3 基础上继续改）：

| 项 | F3 | 现在 | 为什么 |
|---|---|---|---|
| `shadowOffset` | （无此字段） | **2** | 新增。右下 2px 投影，给字垫出对比度 |
| `shadowAlpha` | （无此字段） | **200** | 投影透明度 |
| `outlineWidth` | 1 | 1 | 不变，保留一圈边缘定义 |
| `outlineAlpha` | 190 | **255** | 描边的活是定边界，给满；对照图里 255 明显比 190 有定义感 |

**还没解决的问题**：底板仍是浅色（白 `alpha 80`）、文字仍是白色。
投影和描边都只是在"补"，没有改变"白压白"这个基本面。
真正的解法是二选一——**深色底板配白字**，或者**浅色底板配深字**。
对照图见 `E:\dshwork\宫格外观对比2.png`。

**另一个待定项**：`fontBold` 目前仍是 `true`。8 种字体的对照渲染
（`E:\dshwork\字体对照.png`）显示，**微软雅黑 7pt 加粗是全场最差的一档**，
笔画粘连严重；取消加粗立刻好转。这项还没动，等外观整体定了一起改。

**字体可选方案**（本机装了的）：
- `MiSans` / `Noto Sans SC` —— 字形最好，但**不是 Windows 自带**，分享出去对方大概率没有
  （`new Font()` 遇到缺失字体不会报错，会静默回退成通用字体，很难排查）
- `DengXian` 等线 —— Windows 10+ 自带，比雅黑细、小字号下干净，**推荐做分享版默认值**

### 自用配置：字体描边调整（F3 方案）

`D:\WinQuad\config.json` 的 `style` 段：

| 项 | 改前 | 改后 | 为什么 |
|---|---|---|---|
| `outlineWidth` | 2 | **1** | 2px 纯黑描边把笔画缝隙糊住了，放大看「小飞机」是一团黑底白线 |
| `outlineAlpha` | 255 | **190** | 描边全不透明太重，和浅色底板（`plateAlpha 80`）打架 |
| 其余 | — | 不变 | `textColor` 保持白色、`fontBold` 保持加粗、字号仍 7pt |

**背景**：白字压在浅色底板上，本来就得靠描边才读得出来。
真正干净的解法是换成深色文字（浅底配深字是天然对比，不需要描边），
实测的 F1/F4 两个方案都比现在清爽。但这次先按 F3 走，
保留白色文字的风格，只把描边收细。

**备选方案**（对照图存在 `E:\dshwork\字体方案对比.png`，随时可以换）：
- F1 深字 `#1A1A1A` + 无描边 —— 最干净
- F4 深字 + 底板提到 110 —— 对比最强
- F5 深字 + 关掉加粗 —— 笔画偏细，不建议

### 自用安装的文档补齐

`D:\WinQuad` 之前只有程序，文档没跟上：

- 补 `README.md`（8,155 字节）
- 补 `LICENSE`（35,803 字节）
- 补 `docs\` 三张界面截图
- `使用说明.txt` 从 09:25 旧版换成最新（8,845 字节）

三份文档与 `WinQuad-分享版` 里的逐字节一致（MD5 校验通过）。

---

## v0.1 — 2026-09-13

首个公开测试版。定位是「给朋友试用的测试初版」。
版本号始终是 `0.1.0.0`，中途补进去的改动没有单独发版。

### 核心功能

- **桌面四宫格**：用 `SetParent` 把自绘窗口嵌进 Windows 桌面图标层
  （`SHELLDLL_DefView`），所以它和真图标同层 —— 被所有窗口盖住、
  按 `Win+D` 时还在、不占任务栏、不进 `Alt+Tab`。
- **图标网格实测**：跨进程读 `SysListView32` 里每个图标的真实坐标，
  算出格距（本机 83 × 114）。不读注册表 —— 实测注册表纵向值给 90，和真实值差很多。
- **位置按格位存**：`col` / `row` 而不是像素坐标，换分辨率不会跑偏。
- **拖动吸附 + 自动避让**：松手对齐图标网格；目标格位被占就挪到最近空位。
- **管理器**：三栏图形界面，支持从资源管理器拖放添加程序，
  `.lnk` 自动追溯成真正的 `.exe`（因此图标没有快捷方式小箭头）。
- **参数设置**：一屏可滚动，顶部实时预览，参数超限实时报红。
- **配置带注释**：`config.json` 与 `group-*.json` 里以 `_` 开头的字段就是注释，
  程序忽略。管理器保存时**就地替换数值**，注释和排版原样保留（有回归测试守着）。

### 中途补进 v0.1 的改动

按时间顺序：

1. **改名 DeskGrid → WinQuad**，程序名、exe 名、窗口标题、日志名、注册表自启项名全部跟着改；
   新增旧自启项清理逻辑（改名前设过自启的机器上会残留一个指向老路径的死项）。
2. **README 重写**：去掉 AI 腔（排比句、emoji 小标题、过量加粗与破折号）、
   去掉界面文字描述（只留截图）、许可章节收缩成一行。
3. **应用图标**：白底圆角方块 + 四宫格，左下格是浅蓝底 + 蓝箭头的「快捷方式」格。
   四色沿用红绿黄蓝的骨架但取值重新配过，没有照抄任何厂商的官方品牌色。
   源文件 `assets/icon.svg`，配 `render-icon.ps1` + `make-ico.ps1` 两个可重跑的脚本。
4. **提权加固**：给覆盖层补 `app.manifest` 显式声明 `asInvoker`。
   之前是靠 SDK 默认值（隐式保证）。覆盖层要 `SetParent` 挂到 explorer 的桌面窗口上，
   被提权会被 UIPI 拦掉；管理器提权后则收不到拖放。
5. **许可**：CC BY-NC-SA 4.0 → PolyForm Noncommercial 1.0.0 → MIT → **GPL-3.0**。
   前三者都不合适：CC 不是为软件写的、PolyForm 和 CC 都禁止商用（也就都不是开源协议）、
   MIT 允许别人做成闭源商业产品。GPL-3.0 是「允许商用 + 强制开源」，正好。

### 已知限制

- 只在作者一台机器上验证过（Windows 11 / 1920×1080 / 100% 缩放）。
  换分辨率、换缩放比例、多显示器、其他 Windows 版本都可能出问题。
- 四宫格会把图标缩到 24×24，细节多的图标缩小后可能认不出来，
  建议自行重绘一版小尺寸专用的简化图标。
- 配置格式后续可能还会改，升级时旧配置不保证兼容。
